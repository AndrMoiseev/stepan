interface:
  display_name: "Stepan Router"
  short_description: "Run the repository Stepan pre-development flow"
  default_prompt: "Use $stepan to start or continue this repository's pre-development specification flow."
policy:
  allow_implicit_invocation: true
