## 1. Разделение flow и архитектурные границы

- [x] 1.1 Механически перенести `internal/specflow` в `internal/flows/spec`, обновить импорты, встроенные ресурсы и правила `arch-go.yml`, сохранив поведение документного flow; проверить `go test ./...` и Windows-сборкой `go build -o stepan.exe ./cmd/stepan` на Go 1.26.5.
- [x] 1.2 Создать `internal/flows/impl_loop` с именем пакета `impl_loop` и границы отдельных компонентов доступа к OpenSpec, хранения и выполнения проверок; сохранить независимость flow и общую инфраструктуру вне `flows`; проверить архитектурными тестами допустимые импорты и запрет зависимости одного flow от другого.

## 2. Конфигурация и правила проекта

- [x] 2.1 Добавить чтение раздела `implementation` из пользовательского `~/.stepan/settings.json` и проектного `.stepan/settings.json`, сохранив существующие поля авторизации; проверить временными конфигурациями оба уровня, отсутствие файлов и ошибки JSON без раскрытия авторизации в диагностике.
- [x] 2.2 Реализовать объединение профилей, назначений ролей и отдельных лимитов: приоритет проекта, полная замена профиля и удаление через `null`; ограничить `checks`, `required_checks`, `rules_file` и `main_branch` проектным уровнем; проверить сценарии наследования, удаления используемого профиля и недопустимого пользовательского поля из `implementation-configuration`.
- [x] 2.3 Реализовать определения проверок с `kind`, `command.program`, `args`, `env`, `platforms`, `cwd` и `timeout_seconds`, непустой `required_checks` и дополнительные проверки; проверить полную замену команды по ОС/архитектуре, fallback на общую команду, неизвестные имена и отсутствие обязательного платформенного варианта.
- [x] 2.4 Реализовать разрешение относительных путей от корня репозитория и абсолютных путей без раскрытия `~` и переменных окружения; проверить `cwd` по умолчанию, относительные и абсолютные пути, а также буквальную обработку подстановок.
- [x] 2.5 Реализовать один Markdown `rules_file`, рекурсивный обход его папки и проверку локальных ссылок и изображений относительно каждого документа; проверить вложенные и ссылочные Markdown-ссылки, недоступные цели, `..`, символические ссылки, `.md` за пределами папки правил, исходники внутри/вне репозитория и веб-ссылки без сетевых обращений.
- [x] 2.6 Задать согласованные назначения ролей, численные лимиты, таймауты и предел Explorer по умолчанию без встроенных моделей; проверить значения таблиц design, проектные переопределения и ошибки ссылок на отсутствующие профили.

## 3. Машинное состояние и прикладной журнал

- [ ] 3.1 Определить состояния запуска, иерархию задач, задания, версии брифов, операции, результаты и основания приёмки, включая «принято, ожидает коммита»; проверить допустимые переходы, запрет частичных конечных задач и завершение родителей только после всех детей, без использования галочек Markdown как состояния.
- [ ] 3.2 Реализовать каталог `~/.stepan/runs/<run-id>/`, хранение связанных файлов результатов и ссылок на них с хешами, без автоматической очистки; проверить сохранность незакрытого и закрытого запуска, недоступную ссылку и публикацию файла до события, которое на него ссылается.
- [ ] 3.3 Реализовать основной последовательный JSONL-журнал с принудительным сохранением события до изменения БД и транзакционное представление SQLite с номером последнего применённого события; выбрать SQLite-драйвер с поддержкой сборки без CGO; проверить однократное применение событий и компиляцию хранилища с `CGO_ENABLED=0`.
- [ ] 3.4 Реализовать восстановление SQLite из журнала и связанных файлов, обработку незавершённой последней строки и отказ при повреждении середины; проверить потерю БД и искусственные сбои до/после устойчивой записи JSONL и транзакции SQLite. Не добавлять миграции и гарантии совместимости между изменившимися форматами.
- [ ] 3.5 Реализовать устойчивые счётчики отдельных циклов и консервативную запись начала попытки до внешнего действия; проверить отсутствие общего бюджета задания, сохранение расхода при неопределённом старте, смене модели и перезапуске, а также технические повторы одной операции без повторного расхода семантического раунда.
- [ ] 3.6 Реализовать границы и сбросы счётчиков: новый цикл обязательных проверок после успешного набора и исправлений по ревью, отдельные запросы проверок, уточнения после первого брифа, эпизоды Explorer и финальные раунды; проверить последние допустимые успешные попытки, паузу перед превышением и адресный сброс только причины лимитной паузы.

## 4. Владение рабочей копией и файловые изменения

- [ ] 4.1 Реализовать поиск Git-root, проверку чистого нового старта, текущей ветки, `main_branch` с fallback на `origin/HEAD` и запреты основной ветки/detached HEAD; проверить Git-fixture сценарии неизвестной основной ветки и сохранения существующих изменений без создания или переключения веток.
- [ ] 4.2 Реализовать блокировку управляющего процесса на уровне ОС и ограничение одного незакрытого запуска на рабочую копию, включая паузу; проверить двумя процессами запрет второго управляющего процесса, возможность чтения статуса, освобождение блокировки после выхода и независимость разных репозиториев.
- [ ] 4.3 Использовать и при необходимости расширить `gitsnapshot` для отпечатков файлов, HEAD и индекса перед операциями и сравнения изменений за отдельный вызов; проверить отличие изменений текущего вызова от предыдущих незакоммиченных правок и паузу при неожиданном изменении между операциями.
- [ ] 4.4 Реализовать сохранение и локальное восстановление наблюдаемых запрещённых правок относительно состояния до вызова, отдельный журнал нарушений и сохранение разрешённых правок; проверить нарушение оркестратора, защищённые файлы исполнителя и неоднозначное происхождение/невозможный откат с `execution_blocked`, без общего reset и без требований отложенной строгой изоляции.

## 5. Выполнение команд проверок

- [ ] 5.1 Реализовать прямой запуск `program`/`args` с заданным `cwd`, окружением только данной команды, закрытым stdin и без терминала; проверить тестовым дочерним процессом аргументы с пробелами, отсутствие неявного shell, EOF на stdin и отсутствие утечки `GOOS`/`GOARCH` к следующей команде.
- [ ] 5.2 Реализовать таймаут команды, пользовательское прерывание и завершение дочерних процессов на Windows и macOS с использованием существующего управления процессами там, где применимо; проверить зависшую команду и её дочерний процесс, классификацию неуспеха и сохранение диагностики.
- [ ] 5.3 Реализовать последовательные наборы до первой ошибки: для `checks_requested` только имена в порядке запроса, для приёмки весь `required_checks` в проектном порядке; проверить дополнительную узкую проверку, смешанный запрос без перестановки, неприменимую команду и невыполненный остаток после первой ошибки.
- [ ] 5.4 Сохранять полный stdout/stderr и сведения о проверенном состоянии, возвращая агенту имя, команду, код, длительность и до 20000 символов Unicode с началом/концом и ссылками на полные логи; проверить большой вывод и доступность полных файлов для чтения без записи в машинное состояние.
- [ ] 5.5 Учитывать изменения файлов от сборки, генерации и проверок в diff задания и актуальности приёмки; проверить появление нового файла, изменение ранее одобренного кода, запрещённое изменение защищённого файла и нестабильную генерацию с ограниченными повторами.

## 6. Контракт runtime и три адаптера

- [ ] 6.1 Расширить `agentruntime.ThreadConfig` явным неизменным разрешением записи в workspace при создании сессии, сохранив чтение по умолчанию и отдельный внешний `ArtifactRoot`; проверить валидацию, клонирование конфигурации и прежний документный контракт общими conformance-тестами.
- [ ] 6.2 Реализовать новый режим записи в Codex-адаптере с доступными ограничениями и совместимыми object-схемами без `oneOf`, со всеми properties в required; проверить записи в workspace при разрешении, прежний отказ по умолчанию, работу внешнего ArtifactRoot и прерывание вызова.
- [ ] 6.3 Реализовать новый режим записи в Claude-адаптере, сохранив ограничения документной сессии и жизненный цикл; проверить те же conformance-сценарии чтения/записи, внешнего ArtifactRoot и прерывания на тестовом runtime.
- [ ] 6.4 Реализовать новый режим записи в Nessy-адаптере с сохранением корреляции permissions и авторизации; проверить conformance-сценарии, разрешения активной сессии и отсутствие расширения прав другой сессии.
- [ ] 6.5 Связать профили ролей с фабриками runtime, моделями и reasoning, реализовать предварительную проверку всех ролей цикла и только используемых провайдеров; проверить отсутствие профиля финального ревьюера, неподдерживаемый reasoning и успешную подготовку без установки неиспользуемых адаптеров.

## 7. Ответы, роли и контекст

- [ ] 7.1 Определить схемы ответов каждой роли с `kind`, включая операции оркестратора и предложения bootstrapper, и нормализацию транспортных пустых полей; проверить все допустимые виды результата, неправильную роль/состояние, неизвестный kind и привязку идентификаторов и версий контроллером.
- [ ] 7.2 Подготовить инструкции и сборку стартового контекста ролей: самодостаточный бриф исполнителя/ревьюера, индекс правил с progressive disclosure, контекст оркестратора и независимое финальное ревью; проверить контрактными тестами наличие необходимых данных и отсутствие полной спецификации у исполнителя и истории раундов у финального ревьюера.
- [ ] 7.3 Реализовать владельца сессий: оркестратор на запуск в работающем процессе, брифер/исполнитель/ревьюер на задание, Explorer на запрос и финальный ревьюер на раунд; проверить продолжение после вспомогательного запроса и создание новых сессий на следующую единицу работы.
- [ ] 7.4 Реализовать контролируемый вызов агента с таймаутом 1800 секунд по умолчанию, техническими повторами и постпроверкой файлов; проверить падение, некорректный ответ, таймаут, отмену и повтор после успешного отката нарушения без принятия прежнего ответа.
- [ ] 7.5 Реализовать маршрутизацию Explorer через контроллер с вопросом, границами и результатом исходной сессии, лимитом 10 исследований на эпизод и 12000 символов ответа; проверить запрет делегирования от Explorer, сохранение счётчика после возврата и запрос сокращения в той же сессии в пределах технических попыток.

## 8. OpenSpec, исходные задачи и брифы

- [ ] 8.1 Реализовать доступ к документам выбранного change и основным specs с фиксацией версий входов, не вводя общий интерфейс адаптеров спецификаций и Markdown-парсер задач; проверить загрузку полного пакета, недоступные документы и предоставление основных specs для агентного выбора релевантности.
- [ ] 8.2 Реализовать запуск только нового change либо продолжение собственного сохранённого запуска и извлечение задач оркестратором; проверить формальную структуру, уникальность идентификаторов, сохранение порядка/иерархии и отсутствие импорта чужой частичной реализации или задач закрытого запуска.
- [ ] 8.3 Реализовать исходный полный обязательный набор до первого задания; проверить остановку на первой ошибке с паузой и диагностикой пользователю без вызова исполнителя для автоматического исправления baseline.
- [ ] 8.4 Реализовать выбор брифером непустого начального блока оставшихся конечных задач и создание устойчивого задания; проверить допустимые A, A+B и A+B+C, отказ для B или A+C, повторно выбранной задачи и деления пункта.
- [ ] 8.5 Сохранять бриф отдельным Markdown-файлом на версию с формируемым контроллером YAML frontmatter `assignment_id`, `version`, `task_ids` и ссылкой/хешем в журнале; проверить новый и уточнённый бриф, стабильность задания, одинаковую версию у исполнителя и ревьюера и отсутствие снимков правил.

## 9. Реализация, приёмка и эскалации

- [ ] 9.1 Собрать переходы исполнитель → `checks_requested` → исполнитель и исполнитель → `implementation_ready` → обязательная приёмка; проверить отдельный предел 5 промежуточных запросов на задание, отклонение произвольных команд/параметров и запуск полного набора независимо от прежних успехов.
- [ ] 9.2 Реализовать цикл обязательные проверки → исправления с 3 попытками на цикл и переход к ревью только по успешному актуальному состоянию; проверить начало набора с первой команды после исправления, невозможность коммита с устаревшими результатами и новый цикл проверок после исправлений по ревью.
- [ ] 9.3 Реализовать ревью всего diff задания с результатами проверок и предыдущими замечаниями, включая 3 раунда и `review_disputed`; проверить снятие/сохранение замечания, запрет самостоятельного закрытия исполнителем, основания изменения тестов и отсутствие блокировки из-за предпочтений сверх правил.
- [ ] 9.4 Реализовать уточнение брифа в существующей сессии брифера с 3 попытками после первой выдачи и переоценкой текущего кода; проверить разрешимый пробел, устаревание одобрения по новой версии, невыполнимый исходный порядок и закрытие запуска при существенной проблеме полной спецификации.
- [ ] 9.5 Реализовать `execution_blocked` отдельно от вопросов требований: сохранять диагностику, попытки и требуемое действие пользователя, приостанавливая зависимую работу; проверить проблемы окружения/конфигурации и невозможность автоматически ослабить проверки или изменить scope.

## 10. Коммиты и финальная приёмка

- [ ] 10.1 Реализовать принятие задания с ожиданием коммита и вызов оркестратора для прямого обновления информационного `tasks.md`; проверить сохранение машинных статусов до коммита, отсутствие смысловой сверки галочек и отсутствие лишнего ревью только из-за информационной отметки.
- [ ] 10.2 Принимать непустое обновляемое сообщение из `implementation_ready`, добавлять `Stepan-Run`, `Stepan-Assignment`, `Stepan-Operation` и сохранять ожидаемые родителя, дерево, сообщение и операцию до Git-действия; проверить общий коммит кода и отметок без отдельного агента для сообщения и завершение выбранных задач/родителей только по факту фиксации.
- [ ] 10.3 Реализовать штатный запуск Git hooks и сверку фактического коммита/рабочей копии: отказ оставляет паузу ожидания коммита, изменение результата требует повторной приёмки; проверить hooks-fixtures с отказом и изменением файлов, сохранение созданного коммита без amend/reset и исправляющий новый коммит при необходимости.
- [ ] 10.4 Реализовать итоговый обязательный набор и независимое финальное ревью при отсутствии оставшихся задач; проверить полноту входного контекста, запрет успеха при блокирующих замечаниях и отсутствие push/PR/merge/deployment или ожидания ручных проверок.
- [ ] 10.5 Реализовать добавление оркестратором задач по финальным замечаниям в конец машинного списка и `tasks.md`, затем обычный цикл исправлений; проверить связи с замечаниями, сохранение завершённых статусов, повторную финальную приёмку и паузу перед четвёртым финальным раундом.

## 11. Пауза, продолжение и сбои

- [ ] 11.1 Реализовать пользовательские паузу и закрытие с прерыванием активного агента/команды и устойчивой записью состояния; проверить сохранность коммитов и незакоммиченной работы, отсутствие общего отката, продолжение только паузы и запрет объявления закрытия успехом.
- [ ] 11.2 Реализовать сверку незавершённых Git-операций по ожидаемым данным и служебным строкам коммита; проверить сбои до коммита и после коммита до записи результата, однократное завершение учёта и паузу при неоднозначном сопоставлении вместо дублирования коммита/реализации.
- [ ] 11.3 Реализовать `/resume` со сверкой рабочей копии, спецификации и ручных изменений, автоматическим перечитыванием конфигурации и проверкой папки правил без отдельного подтверждения; проверить новые профили/сессии, невалидную конфигурацию на паузе, изменение scope с закрытием и отсутствие аннулирования приёмки только из-за содержимого правил.
- [ ] 11.4 Выполнять при каждом `/resume` полный обязательный набор с первой команды без расхода любых попыток, до следующей работы; проверить обычную паузу, аварийно прерванную команду, полный набор вместо одиночного повтора и ошибку с сохранением паузы без автоматических исправлений.
- [ ] 11.5 Восстанавливать все агентские сессии после перезапуска заново из сохранённых данных, включая оркестратора, сохраняя незавершённые счётчики и запросы; проверить восстановление без provider-specific истории и различение завершённого вспомогательного запроса и прерванной операции.

## 12. Интерактивный интерфейс

- [ ] 12.1 Подключить `/implement <change>`, `/pause`, `/resume`, `/stop`, `/status` к циклу с меню только допустимых команд; проверить UI-сценариями активную работу, паузу, закрытие и краткое объяснение недоступной команды, введённой вручную.
- [ ] 12.2 При обычном старте приложения находить незакрытый запуск текущей рабочей копии и показывать change, этап, последнее действие и причину остановки; проверить продолжение без run-id и отсутствие автоматического запуска агентов/проверок до `/resume`.
- [ ] 12.3 Отображать роль/проверку, задание, длительность, счётчики, краткие результаты и ссылки на логи без потока внутренних сообщений агентов; проверить обновления прогресса, диагностическую паузу и итог с фактической платформой без ложной runtime-приёмки целевой ОС кросс-сборки.

## 13. Отдельный режим bootstrapper

- [ ] 13.1 Добавить `stepan bootstrap` по текущему репозиторию с отдельной ролью и новой сессией, профилем high по умолчанию и выбором provider/model/reasoning при отсутствии профиля; проверить первый запуск без пользовательских профилей и отсутствие bootstrap-команды в меню текущего цикла.
- [ ] 13.2 Подключить контекст проекта, CI, скриптов и несекретных настроек к bootstrapper с Explorer через контроллер; проверить лимит исследований, запрет самостоятельного запуска проверок и отсутствие существующей авторизации в агентских запросах и диагностике.
- [ ] 13.3 Реализовать проверку предложений для обоих файлов конфигурации, показ diff и сохранение после подтверждения с сохранением авторизации; проверить принятие и отказ, ошибки предложений, сохранение посторонних настроек и завершение режима без автоматического старта реализации.

## 14. Сквозная приёмка и документация

- [ ] 14.1 Добавить сквозной сценарий на временном Git-репозитории с управляемыми агентами: извлечение задач, бриф, дополнительная проверка, исправление, ревью, коммит, финальное замечание и новый проверенный коммит; проверить конечную ветку, машинный учёт, журнал и отсутствие запрещённых внешних Git-действий.
- [ ] 14.2 Проверить восстановление сквозного запуска после сбоев на границах агента, проверки, JSONL/SQLite и Git, включая потерю БД и устаревшую приёмку; подтвердить отсутствие потери разрешённой работы, повторных коммитов, сброса посторонних счётчиков и расхода попыток набора при `/resume`.
- [ ] 14.3 Выполнить общий набор автоматических conformance-сценариев для Codex, Claude и Nessy. По указанию пользователя реальные smoke-тесты агентов автоматически не запускать: подготовить отдельный план или скрипт ручной проверки автономного цикла в одноразовом репозитории, включая запись в проект, структурированные ответы, Explorer и прерывание. Результаты учитывать отдельно по адаптерам; выполнение реального smoke оставить `not_run` до ручного подтверждения, не выдавая fake-runtime тесты за проверку реального провайдера.
- [ ] 14.4 Обновить пользовательскую документацию по `stepan bootstrap`, JSON-конфигурации, платформенным проверкам, правилам, контекстным командам, журналу и восстановлению, а также архитектурные ссылки после переноса flow; проверить примеры валидатором конфигурации и соответствие описанных значений по умолчанию specs.
- [ ] 14.5 Выполнить итоговые `go test ./...`, Windows/amd64-сборку `go build -o stepan.exe ./cmd/stepan` и кросс-сборку macOS/arm64 с `GOOS=darwin`, `GOARCH=arm64`, `CGO_ENABLED=0`, восстановив окружение после команды; зафиксировать фактические результаты и проверить workflow через закреплённый `actionlint@v1.7.12`, если workflow изменялись.
- [ ] 14.6 Провести нативные `go build -o stepan ./cmd/stepan` и `go test ./...` на физическом Apple Silicon Mac и применимые сценарии `docs/changes/features/macos-compatibility/manual-test-plan.md`; зафиксировать runtime-результаты отдельно от кросс-компиляции, не добавляя ручную приёмку в сам автономный цикл.
