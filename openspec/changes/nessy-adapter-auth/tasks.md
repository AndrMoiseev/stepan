## 1. Идентичность и выбор Nessy

- [x] 1.1 Перенести `internal/agentruntime/qwenapp` в `nessyapp`, переименовать внутренние символы, ошибки и импорты, сохранить ACP и processjob; обновить правила `arch-go.yml` и проверить отсутствие старых импортов через `rg` и прохождение архитектурного теста.
- [x] 1.2 Обновить `cmd/stepan/agent_config.go`: принимать `nessy`, отклонять `qwen` с подсказкой миграции и любой явно заданный `--agent-cli-name` для Nessy; табличными тестами проверить обычный запуск, override с другим именем, `nessy`, пустой строкой и старое сочетание `qwen --agent-cli-name nessy`, а также прежние варианты Codex/Claude.
- [x] 1.3 Зафиксировать PATH-команду `nessy` в адаптере, убрать производственный override исполняемого файла, адаптировать fake-launcher tests; проверить разрешение `nessy`, ошибку отсутствия команды и невозможность выбрать другой executable через Config.
- [x] 1.4 Обновить composition root, runtime starters и identity на `nessy`; проверить тестами фабрики и application parity новые метаданные и неизменность выбора Codex/Claude.

## 2. Пользовательская конфигурация

- [x] 2.1 Добавить `internal/usersettings` для чтения `~/.stepan/settings.json` через домашний каталог и разбора `nessy.auth_token`; добавить правило зависимостей и разрешить импорт из `cmd/stepan` и real-CLI тестов, если требуется. Проверить корректный JSON, неизвестные поля, точное сохранение непустого токена и архитектурные ограничения.
- [x] 2.2 Реализовать безопасные ошибки загрузчика; табличными тестами проверить отсутствующий/нечитаемый файл, ошибку домашнего каталога, malformed/trailing JSON, неверные типы объектов и токена, `null`, отсутствующее поле, пустую/пробельную строку и NUL. Все ошибки должны указывать путь и поле без содержимого файла или токена.
- [x] 2.3 Подключить однократное чтение только в Nessy-ветке до сессии/UI; тестами со счётчиком загрузки и starter проверить отсутствие fallback на проектный файл/environment, независимость от cwd, ноль запусков при ошибке и отсутствие чтения настроек для Codex/Claude.
- [x] 2.4 Передать отдельный приватный auth-вход фабрике и `nessyapp.Config.AuthToken`, валидировать его на границе runtime и launcher; проверить отклонение пустого токена до создания процесса и сохранение одного снимка токена при создании нескольких потоков.

## 3. Неинтерактивный запуск и конфиденциальность

- [x] 3.1 Добавить override `NESSY_CLI_DP_AUTH_TOKEN` в единую сборку окружения; тестами проверить конфликт A/B, дубликаты, Windows-регистр, сохранение остальных переменных и отсутствие изменения окружения родителя. Каждый дочерний процесс должен получать ровно одно значение настроенного ключа.
- [x] 3.2 Удалить предварительный интерактивный login и заменить его тесты проверкой прямого ACP-запуска; fake-процессами подтвердить получение токена основным и дополнительными потоками, отсутствие токена в argv/ACP-промптах и отсутствие вспомогательных команд авторизации. При отказе авторизации проверить отсутствие повторного входа/refresh.
- [x] 3.3 Зарегистрировать настроенный токен в `limitedDiagnostic` до старта; проверить сокрытие голого значения, нескольких вхождений, stderr из нескольких записей и токена на границе усечения, включая диагностику ошибки запуска/завершения.
- [x] 3.4 Сохранить безопасную классификацию протокольных ошибок и закрыть передачу токена из строковых ACP-результатов в UI и документы безопасным отказом; fake-тестами с секретом в JSON-RPC ошибке и структурированном результате проверить отсутствие токена в ошибках, событиях, промптах и сохраняемых артефактах flow. Проверить обычный успешный результат без секрета.

## 4. Приёмочные средства и инструкции

- [x] 4.1 Переименовать real-CLI build tag в `nessy_real_cli`, тесты и `scripts/test-qwen-conformance.{ps1,sh}` в Nessy; убрать выбор произвольной команды и использовать домашнюю конфигурацию через общий загрузчик. Проверить компиляцию tagged-тестов, безопасный отказ при отсутствии настроек и provider `nessy` в новых отчётах без токена.
- [x] 4.2 Обновить README и актуальный manual test plan: команда `stepan --agent nessy`, пример JSON с placeholder, ручная замена токена и перезапуск, отсутствие environment fallback и интерактивного login, новые команды runners; проверить соответствие примеров CLI-тестам и отсутствие устаревшего способа запуска в текущих инструкциях.
- [ ] 4.3 Подтвердить на целевой Nessy внешние CLI-флаги и `QWEN_CODE_*` из `process.go` по доступной справке/исходникам и эффективному поведению conformance probes; записать версию Nessy и подтверждённые параметры в актуальном плане приёмки. Не придумывать переименованные переменные и не ослаблять ограничения при несовместимости.

## 5. Общая проверка изменения

- [x] 5.1 На Go 1.26.5 выполнить `go test ./...` и `go build -o stepan.exe ./cmd/stepan` на Windows/amd64; убедиться, что проходят ACP, permissions, structured responses, lifecycle, application parity и архитектурные проверки вместе с новыми auth-тестами. Если затронуты workflow, дополнительно выполнить закреплённый `actionlint` из AGENTS.md.
- [x] 5.2 Выполнить macOS/arm64 cross-build по AGENTS.md и компиляцию tagged Nessy-тестов для darwin/arm64; зафиксировать успех компиляции отдельно от runtime-приёмки.
- [ ] 5.3 Выполнить opt-in реальную приёмку на Windows/amd64 с домашним токеном и конфликтующим унаследованным значением: неинтерактивный запуск, несколько процессов, пять инструментов, запреты, полный flow и завершение дерева. Сохранить безопасный отчёт; missing prerequisites/skip не считать PASS.
- [ ] 5.4 Выполнить нативные сборку/тесты и реальную Nessy-приёмку на физическом Apple Silicon Mac по обновлённому manual test plan, включая окружение потоков и process-group cleanup. Сохранить безопасный отчёт; оставлять задачу открытой, пока реальная проверка недоступна или не пройдена.
