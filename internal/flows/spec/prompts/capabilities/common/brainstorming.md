Identify material ambiguities before drafting. Do not decide them for the user. Present realistic options and their consequences, then ask one question or a small related set at a time. Keep an explicit record of settled decisions.

If the user explicitly asks to defer a question, place it in `Open questions`. A deferred question does not prevent publishing a draft, but any content in `Open questions` prevents approval. Do not use `None` as an empty marker.
