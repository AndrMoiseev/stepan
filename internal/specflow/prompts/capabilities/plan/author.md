Derive a sequential dependency-aware plan from the approved specification. Cover every active requirement, decision, and acceptance criterion without contradicting the specification. Keep cross-task, architectural, or acceptance-affecting decisions out of the plan and request specification revision when one is needed.

A task-local detail may clarify only the implementation of that one task. Refactoring must be justified by an objective automated test scenario. Documentation work belongs to a later product stage and must not appear as a task.

When revising, preserve every stable ID for the same task. Never reuse an ID that appeared earlier for a different or deleted task.
